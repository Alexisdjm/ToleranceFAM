{% comment %} * {
  border: 1px solid red;
} {% endcomment %}

@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@500&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@600&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Roboto&display=swap');

/* -------------------------------------------------------- variables ------------------------------------------------------------ */

:root {
    --font-200: 200;
    --font-300: 300;
    --font-400: 400;
    --font-500: 500;
    --font-600: 600;
    --font-700: 700;
    --bg-green: #073d17;
    --color-lightgray: #646464;
    --button-bg-color: #129F3C;
    --bg-lime-green: #5FC037;
    --color-white: #ffffff;
    --color-black: #000000;
    --font-inter: 'Inter', sans-serif;
    --font-roboto: 'Roboto', sans-serif;
    --font-montserrat: 'Montserrat', sans-serif;
    --page-font-family:'Plus Jakarta Sans', sans-serif;
    --big-headings-font-family: 'Monument Extended', sans-serif;
}

/* -------------------------------------------------------- general classes ------------------------------------------------------ */

.flex {
    display: flex;
}

.flex-center {
    display: flex;
    justify-content: center;
}

.flex-column {
    display: flex;
    flex-direction: column;
}

.align-center {
    align-items: center;
}

.flex-between {
    display: flex;
    justify-content: space-between;
}

.flex-evenly {
    display: flex;
    justify-content: space-evenly;
}

.flex-around {
    display: flex;
    justify-content: space-around;
}

.gap-5 {
    gap: 5px;
}

.gap-10 {
    gap: 10px;
}

.gap-15 {
    gap: 15px;
}

.gap-20 {
    gap: 20px;
}

.basic-aspect-ratio {
    aspect-ratio: 1/1;
}

.basic-section-container {
    width: 100%;
    height: fit-content;
    padding: 50px 0;
}

.grid-col-2 {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
}

.grid-col-3 {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
}

.grid-col-4 {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
}

.centeer {
    justify-content: center;
}

.left {
  justify-content: start;
}

.right {
  justify-content: end;
}

.space-between {
    justify-content: space-between;
}

.relative {
    position: relative;
}

.absolute {
    position: absolute;
}

.hundred-percent {
    width: 100%;
}

.hundred-vw {
    width: 100vw;
}

.centeer {
    justify-content: center;
}

.evenly {
    justify-content: space-evenly;
}

.around {
    justify-content: space-around;
}

.between {
    justify-content: space-between;
}

.text-center {
  text-align: center;
}

.fit-content {
  width: fit-content;
}

/* -------------------------------------------------- footer ---------------------------------------------------------------- */

.footer-block--mixed-flex {
    display: flex;
    gap: 10px;  
}

.footer-block--mixed-img-c h2 {
    font-size: 24px;
    font-weight: var(--font-400);
    line-height: 29px;
    letter-spacing: 0em;
    text-align: left;
}

.footer-block--mixed-text p {
    font-size: 16px;
    font-weight: var(--font-400);
    line-height: 26px;
    letter-spacing: 0em;
    text-align: left;
}

.footer-custom--blocks-container {
    padding: 100px 100px 5rem;
    margin: auto;
}

.footer-custom--linklist {
    width: 25%;
    max-width: 215px;
    margin: 0;
}

.footer-custom--mixed {
    width: 30%;
    max-width: 375px;
    min-width: 365px;
    margin-right: 70px;
}

ul.list-unstyled.list-social {
  flex-direction: column;
  justify-content: flex-start;
  flex-wrap: nowrap;
}

li.list-social__item.footer-custom--list > a {
    gap: 5px;
    display: flex;
    font-size: 16px;
    font-weight: var(--font-400);
    letter-spacing: 0em;
    text-decoration: none;
    padding: 1.1rem 0.8rem !important;

}

li.list-social__item.footer-custom--list p {
    margin: 0;
}

.footer-custom--link-texts {
    color: white;
    font-size: 16px;
    font-weight: var(--font-400);
    letter-spacing: 0em;
    text-decoration: none !important;
    font-family: var(--page-font-family) !important;
}

.footer-custom--link-texts--active {
    font-weight: 500;
}

.footer-block__details-content.list-unstyled li {
    padding: 1.1rem 0.8rem!important;
}

.footer__copyright .copyright__content:first-child {
  font-size: 16px;
  font-weight: var(--font-400);
  text-align: left;
  line-height: 26px;
  letter-spacing: 0em;
  font-family: var(--big-headings-font-family)
}

.footer__copyright .copyright__content:nth-child(2) {
  font-size: 16px;
  font-weight: var(--font-400);
  text-align: left;
  line-height: 26px;
  letter-spacing: 0em;
  font-family: var(--page-font-family) !important;
}

.footer-custom--block-name {
  text-transform: uppercase !important;
}

.footer__blocks-wrapper.grid.between {
  row-gap: 35px;
}

.countdown, .instagram-post {
  overflow: hidden;
}

/* --------------------------------------------------- collection template --------------------------------------------------- */

.custom-page--width {
  width: 100%;
  max-width: 85vw;
  margin: 0 auto;
}

.custom-collection--list {
  display: flex;
  width: 100%;
}

.custom-collection--center {
  display: flex;
  width: 100%;
  justify-content: center;
}

.new-layout--collection-2, .new-layout--collection-3, .new-layout--collection-4 {
  gap: 30px;
  display: grid;
  align-items: center;
  max-width: fit-content;
  justify-content: center;
}

.new-layout--collection-2 {
  grid-template-columns: repeat(2, 1fr)
}

.new-layout--collection-3 {
  grid-template-columns: repeat(3, 1fr)
}

.new-layout--collection-4 {
  grid-template-columns: repeat(4, 1fr)
}

.new-layout--grit-item {
  width: clamp(250px, 20vw, 350px);
}

.card-new--layout {
  gap: 10px;
}

.new-layouth--heading {
  min-height: 47px;
}

/* --------------------------------------------------- header ---------------------------------------------------------------- */

.cart-count-bubble {
  background-color: #5FC037 !important;
  color: var(--color-black) !important;
}

/* --------------------------------------------------- ig slider ------------------------------------------------------------- */

.custom-ig--father {
  position: relative;
}

.custom-ig--slider {
  width: fit-content;
  display: flex;
  overflow: hidden;
  transition: transform .5s;
}

.custom-ig--slide {
  position: relative;
  max-width: fit-content;
  max-height: 252px;
}

.custom-ig--slide svg {
  position: absolute;
  color: var(--color-white);
  width: 20px;
  left: 85%;
  top: 5%;
}

.custom--ig-btn-box { 
  top: 0%;
  z-index: 9;
  width: 100%;
  display: flex;
  position: absolute;
  justify-content: space-between;
}

.ig--btn {
  border: none;
  height: 252px;
  width: 30px;
  opacity: 0;
  background-color: rgba(255, 255, 255, 0.5);
  transition: all 0.3s ease-in-out;
}

.ig--btn:hover {
  opacity: 1;
}

.custom-ig--grandpa {
    width: 100%;
    margin-top: 50px;
    display: flex;
    flex-direction: column;
}

.custom-ig--instagram-content {
  justify-content: space-between;
  display: flex;
  padding: 40px;
  width: 100%;
  max-width: 85%;
  margin: 50px auto 0;
}

.custom-ig--inner-flex {
  display: flex;
  gap: 10px;
  justify-content: flex-start;
}

.custom-ig--text-flex {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 10px;
}

.custom-ig--text-flex h1 {
    font-family: var(--big-headings-font-family) !important;
    font-size: 38px;
    font-weight: var(--font-400);
    line-height: 46px;
    letter-spacing: 0em;
    text-align: left;
    text-transform: uppercase;
    margin: 0;
}

.custom-ig--text-flex p {
    font-family: var(--page-font-family) !important;
    font-size: 16px;
    font-weight: 500;
    line-height: 26px;
    letter-spacing: 0em;
    text-align: left;
    margin: 0;
}

.custom-ig--logo {
  width: 100%;
  height: 100%;
  aspect-ratio: 1/1;
}

.custom-ig--circle-logo {
  width: 100px;
  height:100px;
  padding: 20px;
  border: none;
  border-radius: 50%;
  background-color: var(--color-white);
}

.custom-ig--redirect {
  transition: all 0.3s ease-in-out;
  font-family: var(--page-font-family);
  border: 1.5px solid var(--color-black);
  text-decoration: none;
  letter-spacing: 0.1em;
  height: fit-content;
  padding: 20px 40px;
  line-height: 20px;
  font-weight: 700;
  text-align: left;
  font-size: 16px;
  display: flex;
  color: var(--color-black);
  gap: 15px;
}

.custom-ig--redirect:hover {
  border: 3px solid var(--color-black);
}

.custom-ig--redirect p {
  text-transform: uppercase;
  height: fit-content;
  margin: 0;
}

.custom-ig--redirect svg {
  width: 20px;
  color: var(--color-black);
}

.ig-btn--new-height {
  max-height: 67px;
  min-width: 295px;
}

/* --------------------------------------------------- banner ---------------------------------------------------------------- */

.banner--medium:not(.banner--adapt) {
  min-height: 60rem !important;
}

/* --------------------------------------------------- custom flex section --------------------------------------------------- */


.custom-flex--father {
    width: 100%;
    margin: 50px 0;
    background-size: contain;
}

.short-flex--father {
    margin: 0;
   background-size: contain;
   width: 100%;
}

.short-flex--box {
  padding: 76px 0;
    width: 100%;
}

.custom--flex-box {
    padding: 100px 0;
    width: 100%;
}

.custom-flex--centering {
    display: flex;
    justify-content: center;
    width: 100vw;
}

.custom-flex--main-content-box {
    gap: 65px;
    display: flex;
    width: fit-content;
}

.custom-flex--info-container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 25px;
    max-width: 525px;
}

.custom-flex--info-container h1 {
    font-family: var(--big-headings-font-family) !important;
    font-size: 38px;
    font-weight: var(--font-400);
    line-height: 46px;
    letter-spacing: 0em;
    text-align: left;
    color: var(--color-white);
    margin: 0;
    height: fit-content;
    text-transform: uppercase;
}

.custom-flex--info-container p {
  font-family: var(--page-font-family);
  font-weight: var(--font-400);
  color: var(--color-white);
  letter-spacing: 0em;
  line-height: 26px;
  text-align: left;
  font-size: 16px;
  max-width: 500px;
  margin: 0;
}

.custom-flex--img-box {
  width: 470px;
  height: 490px;
  aspect-ratio: 1.042/1;
  overflow: hidden;
}

.custom-flex--img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.no-presence {
    display: none;
}

.heading-img {
    display: inline-block;
}

.custom-flex--btn {
  border: 1.5px solid var(--color-white);
  font-family: var(--page-font-family);
  transition: all 0.3s ease-in-out;
  font-weight: var(--font-700);
  text-transform: uppercase;
  color: var(--color-white);
  letter-spacing: 0.1em;
  text-decoration: none;
  line-height: 20.16px;
  padding: 20px 36px;
  width: fit-content;
  margin-top: 25px;
  text-align: left;
  font-size: 16px;
  display: block;
}

.custom-flex--btn:hover {
  background-color: var(--color-white);
  color: var(--color-black);
}

.custom-flex--headingg {
  gap: 15px;
  display: flex;
  align-items: center;
  justify-content: space-around;
}

.color-black-font, .color-black-font h1, .color-black-font p {
  color: var(--color-black);
}

.color-white-font, .color-white-font h1, .color-white-font p {
  color: var(--color-white);
}

.border-new-black {
  border: 2px solid var(--color-black);
}

.border-new-black:hover {
  border: 3px solid var(--color-black);
}

.border-new-white {
  border: 2px solid var(--color-white);
}

.border-new-white:hover {
  border: 3px solid var(--color-white);
}

.button-height {
  height: 64px;
}

h1.margin-d-20 {
  margin: 0 0 20px;
}

.margin-up-40 {
  margin: 40px 0 0;
}

/* --------------------------------------------------- collection grid ------------------------------------------------------- */

.featured-collections__col {
  position: relative;
}

.featured-collections__col .featured--new-title  {
  text-decoration: none;
}

.featured--new-title {
  margin: 0;
  font-size: 32px;
  font-style: normal;
  line-height: normal;
  color: var(--color-white);
  text-transform: uppercase;
  font-weight: var(--font-500);
  transition: all 0.3s ease-in-out;
  font-family: var(--big-headings-font-family);
  position: absolute;
  bottom: 50px;
  left: 50px;
}

.featured-collections__col:hover > .featured--new-title {
  transform: scale(1.1);
}

/* --------------------------------------------------- custom slider --------------------------------------------------------- */

.custom-slider--father {
  width: 100vw;
  margin: 50px 0;
}

.custom-slider--main-container {
  flex-direction: column;
  padding: 50px 0;
  display: flex;
  width: 100%;
  gap: 48px;
}

.align-gap--reviews, .align-gap--products, .align-gap--collections {
  gap: 50px;
}

..align-gap--videos {
  gap: 25px;
}

.custom-slider--top-content {
  width: 100%;
  max-width: 85%;
  margin: 0 auto;
}

.custom-slider--top-content h1 {
  font-family: var(--big-headings-font-family) !important;
  text-transform: uppercase;
  font-size: 38px;
  font-weight: 400;
  line-height: 46px;
  letter-spacing: 0em;
  text-align: left;
  margin: 0;

}

.custom-slider--top-content {
  display: flex;
  width: 100%;
  align-items: center;
  justify-content: space-between;
}

.custom-slider--arrows-box {
  display: flex;
  min-width: 120px;
  justify-content: space-between;
}

.custom-slider--arrows {
    padding: 5px;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: transparent;
    border: 2px solid #129F3C;
  transition: all 0.3s ease-in-out;
}

.custom-slider--arrows:hover {
  border: 2px solid var(--color-black);
}

.custom-slider--first-slider-container {
  display: flex;
}

.custom-slider--slider {
  overflow: hidden;
  width: fit-content;
}

.first-width--container {
  max-width: 70vw;
}

.second-width--container{
  max-width: 79vw;
}

.third-width--container{
  max-width: 80vw;
}

.forth-width--container{
  max-width: 88vw
}

.fifth-width--container{
  max-width: 89vw;
}

.custom-slider--inner-box {
  display: flex;
  overflow: hidden;
  width: fit-content;
  max-width: fit-content;
  transition: all 0.3s ease-in-out;
  gap: 25px;
}

.custom-product--cart:hover .product-pic {
  transform: scale(1.1)
}

.custom-product--cart:hover .custom-product--cart--text a {
  text-decoration: underline;
}

.product-pic--box {
  width: 266px;
  aspect-ratio: 1/1.3;
  overflow: hidden;
  position: relative;
}

.product-pic {
  height: 100%;
  width: 100%;
  object-fit: contain;
  transition: all 0.3s ease-in-out;
}

.custom-product--cart p {
  font-family: var(--page-font-family);
  font-size: 16px;
  font-weight: 500;
  line-height: 26px;
  letter-spacing: 0em;
  text-align: left;
  margin: 8px 0 0;
}

.custom-product--cart--text a {
  font-family: var(--big-headings-font-family) !important;
  font-weight: var(--font-400);
  color: var(--color-black);
  text-decoration: none;
  letter-spacing: 0em;
  line-height: 19px;
  text-align: left;
  font-size: 16px;
  display: block;
  margin: 0;
}

.reviews-block--flex-column {
  display: flex;
  flex-direction: column;
}

.reviews-block--flex-column h1 {
  font-size: 16px;
  font-weight: var(--font-400);
  line-height: 19px;
  letter-spacing: 0em;
  text-align: left;
  margin: 0;
}

.reviews-block--flex-column p {
  font-family: var(--page-font-family);
  font-size: 16px;
  font-weight: var(--font-500);
  line-height: 26px;
  letter-spacing: 0em;
  text-align: left;
}

.reviews-block--container {
  min-width: 340px;
  max-width: 350px;
  border: 1.5px solid var(--color-black);
  padding: 20px 15px;
}

.profile-pic {
  width: 55px;
  height: 55px;
  aspect-ratio: 1/1;
}

.reviews-block--profile.flex h3, .reviews-block--profile.flex p {
  margin: 0;
}

.stars {
  width: 60%;
  margin-bottom: 15px;
}

.custom-collection--card {
  width: 266px;
  padding: 20px;
  display: flex;
  position: relative;
  aspect-ratio: 1/1.3;
  flex-direction: column;
  justify-content: end;
  background-size: cover;
  text-decoration: none;
  background-repeat: no-repeat;
}

.custom-collection--card::before {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 90%;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0));
  pointer-events: none; /* Permite hacer clic a través del filtro */
}

.custom-collection--card h1 {
  font-family: var(--big-headings-font-family);
  font-weight: var(--font-400);
  text-transform: uppercase;
  color: var(--color-white);
  letter-spacing: 0em;
  text-align: center;
  line-height: 19px;
  font-size: 16px;
  position: relative;
  margin: 0;
  transition: all 0.3s ease-in-out;
}

.custom-collection--card:hover h1 {
  transform: scale(1.1)
}

.dots-box {
  gap:0;
  display:flex;
  justify-content: center;
}

.dots-box span {
  width: 40px;
  height: 4px;
  background-color: lightgray;
  transition: background-color 0.3s ease-in-out;
}

.activee {
  background-color: var(--color-black) !important;
}

.portrait-pic {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.portrait-container {
  min-width: 266px;
  max-height: 445px;
}

/* --------------------------------------------------- collection page ------------------------------------------------------- */

.new-grid--align {
  gap: 20px !important;
}

.new-grid--align .new-grid--item {
  max-width: 266px;
}

.new-cart--inner {
  min-height: 366px !important;
}

.new-collection--title {
  font-family: var(--big-headings-font-family);
  font-weight: var(--font-400);
  text-transform: uppercase;
  letter-spacing: 0em;
  line-height: 58px;
  text-align: left;
  font-size: 52px;
  margin: 0;
}


.new-collection--grid-heading {
  display: flex;
  gap: 15px;
  margin: 30px 0;
  flex-direction: column;
}

.new-collection--grid-heading p {
  font-family: var(--page-font-family);
  font-weight: var(--font-400);
  letter-spacing: 0em;
  line-height: 21px;
  text-align: left;
  font-size: 16px;
  color: var(--color-lightgray);
  margin: 0;
}

.facets-vertical.page-width, .facets.facets-vertical-sort.page-width {
  padding: 0;
}

.facets.facets-vertical-sort.page-width {
  padding: 0 !important;
}

.facets-vertical-form {
  justify-content: start !important;
}

.facets-vertical-form > div {
  margin: 0 !important;
}

.new-collection--product-text {
  font-family: var(--page-font-family) !important;
  font-weight: var(--font-600);
  letter-spacing: 0em;
  line-height: 19px;
  text-align: left;
  font-size: 16px;
}

.new-collection--product-text strong {
  font-weight: var(--font-400) !important;
}

.pathh span:first-child {
  margin-right: 10px;
}

.pathh span:nth-child(2) {
  margin-left: 10px;
}

aside.facets-wrapper {
  margin-top: 30px;
}

#ProductCountDesktop {
  display: block;
  margin-bottom: 25px;
  color: #C1C1C1 !important;
}

.new-collectio--filster-tag {
  font-family: var(--page-font-family) !important;
  font-weight: var(--font-500);
  background-color: #F0F0F0;
  letter-spacing: 0.07em;
  line-height: 19px;
  text-align: left;
  font-size: 16px;
  display: block;
  padding: 15px;
  color: var(--color-lightgray);
}

.facets__form-vertical {
  width: 300px !important;
}

.active-facets__button-wrapper span {
  font-family: var(--page-font-family) !important;
  font-weight: var(--font-700);
  text-transform: uppercase;
  letter-spacing: 0.07em;
  line-height: 19px;
  text-align: left;
  font-size: 16px;
}

.new-collection--Color-filter {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  margin-left: -12.5px;
  row-gap: 17.5px;
}

.new-collection--column-color {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 55px;
  gap: 7px;
}

.custom-color--checkbox {
   position: absolute;
   opacity: 0;
   cursor: pointer;
   height: 0;
   width: 0;
}

.checkmark {
  position: relative;
  top: 0;
  left: 0;
  height: 35px;
  width: 35px;
  border-radius: 25px;
  transition: 0.15s;
}

.custom-color--checkbox:checked ~ .checkmark {
  border: 3px solid var(--bg-lime-green);
}

div.checkmark:empty {
  display: block !important;
}

.new-cart--badge {
  position: absolute;
  top: 0;
}

.new--badge {
  margin: 0;
  font-size: 14px;
  border-radius: 0;
  padding:8px 16px;
  line-height: 17px;
  text-align: center;
  letter-spacing: 0em;
  color: var(--color-white);
  text-transform: uppercase;
  font-weight: var(--font-400);
  font-family: var(--big-headings-font-family);
}

.custom-collection--full-unstyled-link {
  font-family: var(--big-headings-font-family);
  font-weight: var(--font-400);
  text-transform: uppercase;
  color: var(--color-black);
  text-decoration: none;
  letter-spacing: 0em;
  line-height: 19px;
  text-align: left;
  font-size: 16px;
}

.price-item.price-item--regular, .price-item.price-item--sale.price-item--last {
  font-family: var(--page-font-family);
  color: var(--color-black);
  letter-spacing: 0em;
  line-height: 26px;
  text-align: left;
  font-size: 16px;
}

.price-item.price-item--regular {
  font-weight: var(--font-500);
}

.price-item.price-item--sale.price-item--last {
  font-weight: var(--font-600);
}

.flex-between.upper-belt {
  position: relative;
  z-index: 99;
  margin: 10px 0;
}

.card__information {
  padding-top: 0 !important;
}

.radio-colors--container {
  display: flex;
  gap: 7px;
  flex-wrap: wrap;

}

.custom-radio {
  display: block;
}

.custom-radio--circle {
  height: 20px;
  width: 20px;
  padding: 5px;
  border-radius: 50%;
  cursor: pointer;
  display: block;
}

input[type="radio"]:checked + label span.custom-radio--circle {
    border: 2px solid var(--bg-lime-green);
}

.custom-hidden {
  width: 1px;
  height: 1px;
  visibility: hidden;
  position: absolute;
}

.quick-add {
  border: none;
  cursor: pointer;
  width: fit-content;
  height: fit-content;
  background-color: transparent;
  transition: opacity 0.3s ease-in-out;
  height: fit-content !important;
  padding: 0 !important;
  display: flex;
}

button.quick-add.disabled {
  opacity: 1 !important;
}

.quick-add.active.disabled svg {
  fill: var(--button-bg-color) !important;
}

.quick-add.active.disabled path {
  stroke: var(--button-bg-color) !important;
}

.quick-add:hover ~ svg {
  transform: scale(1.1) !important;
}

.quick-add:disabled {
  opacity: 0.4;
}

.product-price--grid s.price-item.price-item--regular {
  font-family: var(--page-font-family);
  font-size: 16px !important;
  font-weight: var(--font-500) !important;
  line-height: 26px;
  letter-spacing: 0em;
  text-align: left;
}

.product-price--grid span.price-item.price-item--sale.price-item--last {
  font-family: var(--page-font-family);
  font-size: 18px !important;
  font-weight: var(--font-700) !important;
  line-height: 26px;
  letter-spacing: 0em;
  text-align: left;
}

span.facet-checkbox__text, .facets__summary-label, .facets__summary.caption-large span {
  font-family: var(--page-font-family) !important;
  font-weight: var(--font-700) !important;
  font-size: 16px !important;
  letter-spacing: 0.07em;
  line-height: 19px;
  text-align: left;
}

label.facets__label.facet-checkbox > svg {
  width: 2.6rem;
  height: 2.6rem;
  border: 1px solid var(--color-black);
}

label.facets__label.facet-checkbox > svg > rect {
  stroke-width: 0;
}

label.facets__label.facet-checkbox svg.icon.icon-checkmark {
  width: 2.3rem !important;
  height: 1.2rem !important;
  top: 1.7rem;
  left: 0.1rem;
}

label.facets__label.facet-checkbox > input:checked + svg {
  background-color: var(--button-bg-color);
  border: 1px solid var(--button-bg-color);
}

label.facets__label.facet-checkbox > input:checked + svg + svg {
  background-color: var(--button-bg-color);
  border: 1px solid var(--button-bg-color) !important;
  color: var(--color-white);
}



/* --------------------------------------------------- product page ------------------------------------------------------------ */

.thumbnail-list.slider--tablet-up .thumbnail-list__item.slider__slide {
  width: 172px !important;
  height: 180px !important;
}

.product__media.media.media--transparent img {
  object-fit: contain !important;;
}

.thumbnail-slider.slider-mobile-gutter {
  margin-left: calc(100% - 578px);
}

.custom-simple--card {
  max-width: 266px;
}

.custom-simple--card p {
  margin: 0 !important;
}

.title-cart-mix {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 15px;
}

.simple-product-pic {
  width: 266px;
  aspect-ratio: 1/1.3;
  object-fit: cover;
}

.new-btn--related-p {
  font-family: var(--page-font-family);
  background-color: var(--button-bg-color);
  font-weight: var(--font-500);
  letter-spacing: 0.1em;
  padding: 0 36px;
  text-align: center;
  line-height: 20px;
  font-size: 16px;
  color: var(--color-white);
  text-transform: uppercase;
  text-decoration: none;
  border: 3px solid var(--button-bg-color);
  transition: all 0.3s ease-in-out;
  min-height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.new-btn--related-p:hover {
  border: 3px solid var(--color-black);
}

.related-products__heading.inline-richtext {
  margin: 0 !important;
  display: flex;
  align-items: center;
}

.custom-rating--container {
  display: flex;
  width: fit-content;
  gap: 15px;
}

.custom-rating--container p {
  font-family: var(--page-font-family);
  font-weight: var(--font-500);
  letter-spacing: 0em;
  line-height: 24px;
  text-align: left;
  font-size: 16px;
  color: #494E55;
  margin: 0;
}

.available-warning, .unavailable-warning {
  display: flex;
  width: fit-content;
  align-items: center;
  gap: 5px;
}

.available-warning p, .unavailable-warning p {
  font-family: var(--font-montserrat);
  font-weight: var(--font-500);
  font-size: 16px;
  line-height: 24px;
  letter-spacing: 0em;
  text-align: left;
}

.available-warning p {
  color: var(--button-bg-color);
}

.unavailable-warning p {
  color: red;
}

.available-warning span, .unavailable-warning span {
  display: block;
  border-radius: 50%;
  width: 9px;
  height: 9px;
}

.available-warning span {
  background-color: var(--button-bg-color);
}

.unavailable-warning span {
  background-color: red;
}

.custom-stars {
  max-width: 128px;
}

.new-settings-title h1 {
  font-size: 35px !important;
  text-transform: uppercase;
  line-height: 42px !important;
  letter-spacing: 0em !important;
  font-weight: var(--font-400) !important;
}

.price-item.price-item--sale.price-item--last {
  font-family: var(--page-font-family);
  font-weight: var(--font-700);
  color: var(--color-black);
  letter-spacing: 0.07em;
  line-height: 29px;
  text-align: left;
  font-size: 24px;
}

span s.price-item.price-item--regular {
  font-family: var(--page-font-family);
  font-weight: var(--font-400);
  letter-spacing: 0.07em;
  line-height: 29px;
  text-align: left;
  font-size: 24px !important;
  color: var(--color-lightgray) !important;
}

.custom-icons-info-container {
  display: flex;
  flex-direction: column;
}

.row-container {
  padding: 20px 80px;
  border: 0.5px solid lightgray;
  margin: -0.5px 0;
  display: flex;
  gap: 10px;
}

.custom-icon--shape {
  height: 40px;
  width: 40px;
}

.content-box-new {
  display: flex; 
  flex-direction: column;
  gap: 10px;
}

.content-box-new h4, .content-box-new h3, .content-box-new h2, .content-box-new h1 {
  font-family: var(--page-font-family) !important;;
  font-weight: var(--font-700);
  font-size: 16px;
  line-height: 24px;
  letter-spacing: 0em;
  margin: 0 !important;

}

.content-box-new p {
  font-family: var(--page-font-family);
  font-weight: var(--font-500);
  font-size: 12px;
  line-height: 18px;
  letter-spacing: 0em;
  margin: 0;
}

.content-box-new p a {
  color: var(--color-lightgray);
}

.product__description.rte.quick-add-hidden {
font-family: var(--page-font-family) !important;
font-weight: var(--font-400);
font-size: 16px;
line-height: 27px;
letter-spacing: 0em;
text-align: left;
color: var(--color-lightgray);

}

.quantity-checkout--container {
  display: flex;
  justify-content: space-between;
  margin: 20px 0;
}

.wishlist-btn-box {
  display: flex;
  min-width: 70px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.wishlist-btn {
  padding: 15px !important;
  border: 1px solid #EDEDED;
  color: var(--button-bg-color);
  background-color: transparent;
  transition: all 0.3s ease-in-out;
  height: fit-content !important;
  max-height: 60px;
  margin: auto 0;
}

.wishlist-btn:hover {
  border: 3px solid var(--color-black);
  margin: auto -3px;
}

.product-form__input.product-form__quantity {
  max-width: fit-content;
}

.new-btn--class {
  display: block !important;
  padding: 16px 36px;
}

.shopify-payment-button__button.shopify-payment-button__button--unbranded, .new-btn--class {
  font-family: var(--page-font-family) !important;
  background-color: var(--button-bg-color);
  border: 3px solid var(--button-bg-color);
  text-transform: uppercase !important;
  transition: all 0.3s ease-in-out;
  font-weight: var(--font-700);
  font-size: 16px !important;
  letter-spacing: 0.1em;
  max-width: 320px;
  line-height: 20px;
  text-align: center;
  width: 320px;

}

.shopify-payment-button__button.shopify-payment-button__button--unbranded:hover, .new-btn--class:hover {
  background-color: var(--button-bg-color) !important;
  border: 3px solid var(--color-black);
}

.price-per-item__container {
  height: 56px !important;;
}

quantity-input.quantity {
  height: 100% !important;
}

.product-form__input input[type=radio]+label {
  padding: 0 !important;
  width: 35px;
  height: 35px;
  border: 3px solid var(--color-white) !important;
}

.product-form__input input[type=radio]:checked+label {
  box-shadow: 0 0 0 2px var(--button-bg-color) !important;
}

.variant-name--custom {
  font-family: var(--page-font-family) !important;
  font-weight: var(--font-700);
  color: var(--color-black);
  font-size: 16px !important;
  line-height: 19px;
  letter-spacing: 0em;
  text-align: left;
}

.flex-select--align {
  width: 100%;
  display: flex;
  align-items: center;
  padding-right: 10px;
  justify-content: space-between;
}

.sizing-announcement p {
  font-family: var(--page-font-family);
  color: var(--color-lightgray);
  font-weight: var(--font-400);
  letter-spacing: 0em;
  line-height: 21px;
  text-align: left;
  font-size: 16px;
  margin: 0;
}

.product-collection--origin {
  margin: 50px 0;
  padding: 0 24px;
}

.product-collection--origin span {
  align-items: center;
  display: flex;
  gap: 15px;
}

.product-collection--origin p {
  font-family: var(--page-font-family);
  color: var(--color-lightgray);
  font-weight: var(--font-400);
  letter-spacing: 0em;
  width: fit-content;
  line-height: 21px;
  text-align: left;
  font-size: 14px;
  margin: 0;
}

/* --------------------------------------------------- new email form -------------------------------------------------------- */

.font-white p, .font-white h1, .font-white h2, .font-white h3 {
  color: var(--color-white);
}

.font-black p, .font-black h1, .font-black h2, .font-black h3 {
  color: var(--color-black);
}

.new-email--flex-box {
    display: flex;
    justify-content: center;
    padding: 120px 0;
}

.new-email--father {
  background-size: cover;
  background-repeat: no-repeat;
}

.new-email--first-flex {
    display: flex;
    align-items: center;
    gap: 100px;
}

.new-email--first-flex-column {
  flex-direction: column;
  align-items: center;
  display: flex;
  gap: 30px;
}

.new-email--first-flex-column h1, .new-email--first-flex-column p {
  text-align: center !important;
}

.new-email--flex-container {
  max-width: 550px;
}

.new-email--flex-container h1 {
  font-family: var(--big-headings-font-family);
  font-size: 32px;
  font-weight: 400;
  line-height: 38px;
  letter-spacing: 0em;
  text-align: left;
}

.new-email--flex-container p {
  font-family: var(--page-font-family);
  font-size: 16px;
  font-weight: 400;
  line-height: 26px;
  letter-spacing: 0em;
  text-align: left;
}

.new-email--submit-btn {
  background-color: #129F3C;
  color: var(--color-white);
  text-transform: uppercase;
  min-width: 175px;
  border: none;
}

form.newsletter-form {
  max-width: 800px !important;
  width: 540px !important;
}

div.newsletter-form__field-wrapper {
  max-width: 600px !important;
}



/* --------------------------------------------------- related products ------------------------------------------------------ */

.new-btn--related-itself {
  margin-bottom: 49px;
}

h2.custom-related--title {
  font-family: var(--big-headings-font-family);
  font-weight: var(--font-400);
  font-size: 38px;
  line-height: 46px;
  letter-spacing: 0em;
  text-align: left;
}

/* --------------------------------------------------- custom simple gallery ------------------------------------------------- */
.samples-container--gallery {
  gap: 0;
  flex-wrap: nowrap;
}

.custom-simple--gallery-img-box {
  overflow: hidden;
  position: relative;
  aspect-ratio: 1/1;
  width: 25.1vw;
  margin: 0 -1px;
}

.custom-simple--gallery-img-box:hover .product-pic {
  transform: scale(1.05);
}

.custom-simple--gallery-father {
  margin: 0;
}

/* --------------------------------------------------- new flex complex section ---------------------------------------------- */

.new-flex--father {
  width: 100%;
  padding: 120px 0;
}

.new-flex--inner {
  gap: 80px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.new-flex-content-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: fit-content;
  max-width: 45vw;
  gap: 30px;
}

.new-flex-content-box h1 {
  font-family: var(--big-headings-font-family);
  font-weight: var(--font-400);
  font-size: 38px;
  line-height: 46px;
  letter-spacing: 0em;
  text-align: center;
  margin: 0;
  text-transform: uppercase;
}

.new-flex-content-box p {
  font-family: var(--big-headings-font-family);
  color: var(--color-lightgray);
  font-weight: var(--font-400);
  font-size: 16px;
  line-height: 21px;
  letter-spacing: 0em;
  text-align: center;
  margin: 0;
}

.new-flex--flex-elements {
  width: 100%;
  display: flex;
  padding: 0 30px;
  justify-content: center;
}

.new-flex--direct-elements-box {
  display: flex;
  gap: 3.54vw;
  width: fit-content;
  max-width: 1140px;
}

.main-flex-image--box {
  position: relative;
  overflow: hidden;
  min-width: 38vw;
  aspect-ratio: 1.04/1;
}

.main-flex-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.flex-blocks-align {
  display: flex;
  flex-direction: column;
  gap: 25px;
}

.grid-blocks-align {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  max-width: 520px;
  height: fit-content;
  row-gap: 4.3vw;
  column-gap: 3vw;
}

.block-flex-container {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.complex-flex-card-width {
  max-width: 236px;
}

.block-flex-container h3 {
  font-family: var(--font-inter) !important;
  font-weight: var(--font-600);
  color: var(--color-black);
  font-size: 20px;
  line-height: 26px;
  letter-spacing: 0em;
  text-align: left;
  margin: 0;
}

.block-flex-container p {
  font-family: var(--font-roboto) !important;
  color: var(--color-lightgray);
  font-weight: var(--font-400);
  font-size: 18px;
  line-height: 27px;
  letter-spacing: 0em;
  text-align: left;
  margin: 0;
}

.icon-flex-pic {
  width: 40px;
  aspect-ratio: 1/1;
}

.banner__text.rte.caption-with-letter-spacing {
  font-family: var(--page-font-family);
  font-size: 16px;
  font-weight: var(--font-700);
  line-height: 20px;
  letter-spacing: 0.2em;
  text-align: center;
  color: var(--color-white);
  text-transform: uppercase;
  margin: 0 0 40px;

}

/* --------------------------------------------------- countdown ------------------------------------------------------------- */

#ctb_placeholder #epb_background {
  min-width: 500px  !important;
  height: 96px !important;
}

li.flip-clock-active div.up div.inn, li.flip-clock-active div.down div.inn,
li.flip-clock-before div.up div.inn, li.flip-clock-before div.down div.inn {
  font-family: var(--big-headings-font-family) !important;
  font-size: 32px !important;
}

div.flip-clock-wrapperht ul.flip {
  border-radius: 0 !important;
  margin: 3.2px 0 !important;
}

span.flip-clock-dotht.top, span.flip-clock-dotht.bottom {
  visibility: hidden;
}

.flip-clock-dividerht .flip-clock-label {
  top: 70px !important;
}

.flip-clock-wrapperht ul li a div div.inn, .flip-clock-wrapperht ul li a div.down {
  border-radius: 0 !important;
}

.flip-clock-wrapperht .flip {
  box-shadow: none !important;
}

a.countdown-text--custom {
  font-family: var(--page-font-family);
  border: 2px solid var(--color-white);
  font-weight: var(--font-700);
  color: var(--color-white);
  letter-spacing: 0.1em;
  line-height: 20px;
  text-align: left;
  font-size: 16px;
  display: block;
  padding: 20px 36px;
  height: fit-content;
  text-decoration: none;
  transition: all 0.3s ease-in-out;
}

a.countdown-text--custom:hover {
  border: 3.5px solid var(--color-white);
}

.countdown__inner-container {
  align-items: center;
}

.countdown-btn {
  min-width: 205px;
  min-height: 65px;
}

/* --------------------------------------------------- media queries --------------------------------------------------------- */

@media only screen and (max-width: 463px) {
  .footer-custom--mixed {
    width: 100%;
    max-width: 100%;
    min-width: fit-content;
    margin: 0;
  }

  .footer-custom--blocks-container {
      padding: 100px 10px 5rem;
  }

  .footer-block--mixed-flex h2 {
      font-size: 22px;
  }

.custom-ig--instagram-content {
    gap: 40px;
  }

   .custom-ig--text-flex h1 {
    font-size: 20px;
  }
    
  .custom-ig--circle-logo {
    width: 80px;
    height: 80px;
  }

  .custom-flex--info-container h1 {
    font-size: 25px;
  }

  .heading-img {
    width: 40vw;
  }

  .custom-flex--headingg {
    margin-bottom: 15px;
  }

  .banner__heading.inline-richtext.h2 {
    font-size: 40px !important;
  }

  .new-flex--inner {
    gap: 50px;
  }

  form.newsletter-form {
    width: auto !important;
    padding: 0 20px;
  }

  div.field label.field__label {
    font-size: 12px !important;
  }

  div.field input.field__input {
    padding: auto !important;
  }

  .custom-slider--top-content h1 {
    font-size: 24px; 
    line-height: 30px;
  }

  h2.banner__heading {
    font-size: 42px;
    line-height: 44px ;
  }

  .custom-slider--father {
    margin: 0;
  }

  .new-email--flex-container h1 {
    font-size: 28px;
  }

  .forth-width--container {
    max-width: 93vw;
  }

  h3.countdown__heading {
    width: auto;
    font-size: 28px !important;
  }

  .custom-slider--arrows-box {
    min-width: 100px;
  }

  aside.facets-wrapper {
    margin-top: 0;
  }

  .new-grid--align {
    flex-direction: column;
    align-items: center;
  }

  .new-grid--item {
    width: 100% !important;
  }

  .new-collection--title {
    font-size: 40px;
    line-height: 44px;
  }

  .quantity-checkout--container {
    flex-direction: column;
  }

  .product-form__input.product-form__quantity {
    width: 100%;
    max-width: 100% !important;
    margin: 0;
  }

  quantity-input.quantity {
    width: 100%;
  }

  .product-form__submit.new-btn--class {
    width: 100%;
    max-width: 100%;
  }

  .wishlist-btn {
    gap: 15px;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;  
    margin: 0;
  }

  .wishlist-btn span {
    font-size: 16px;
    text-transform: uppercase;
    color: var(--color-black);
  }

  .quantity-checkout--container {
    gap: 15px;
  }

  .product-form__submit.new-btn--class {
    margin: 0;
  }

  product-form.product-form {
    margin: 0 !important;;
  }

  .row-container {
    padding: 20px 50px;
  }

  h2.custom-related--title {
    font-size: 28px;
    line-height: 30px;
  }

  .new-layout--collection-mobile-2 {
    grid-template-columns: repeat(2, 1fr) !important;
  }

  .new-layout--collection-mobile-1 {
    grid-template-columns: repeat(1, 1fr) !important;
  }

  .new-layout--collection-mobile-2 .new-layout--grit-item {
    width: clamp(160px,40vw,350px) !important;
  }

  .new-layouth--heading {
    font-size: 14px !important;
  }

  .new-layout--collection-2, .new-layout--collection-3, .new-layout--collection-4 {
    column-gap: 20px;
    row-gap: 0;
  }

  .featured-collections__inner-container.desktop-tablet--layout {
    display: none;
  }

  .featured-collection--mobile-layout {
    display: flex;
    flex-direction: column;
    max-width: 90vw;
    gap: 15px;
    margin: 75px auto 50px;
  }

  .featured-mobile-new--box {
    width: 90vw;
    margin: 0 auto;
    aspect-ratio: 16/9;
    position: relative;
  }

  .featured-mobile-new--box .featured-collections__img {
    object-position: 0 -40px;
  }

  .featured-mobile-new--box .featured--new-title {
    left: 0;
    bottom: 10px;
    margin: 0 0 0 10px !important;
  }

  .new-collection--title {
    font-size: clamp(30px, 8vw, 40px);
  }
}

@media only screen and (min-width: 464px) and (max-width: 767px) {
    .footer-block--mixed-flex h2 {
        font-size: 28px;
    }

    .custom-ig--text-flex h1 {
        font-size: 28px;
    }

    .custom-ig--instagram-content {
        gap: 20px;
    }

  .banner__heading.inline-richtext.h2 {
    font-size: 44px !important;
  }

  .shopify-payment-button__button.shopify-payment-button__button--unbranded, .new-btn--class {
    max-width: 50vw;
  }

  .custom-slider--top-content h1 {
    font-size: 28px;
  }

  form.newsletter-form {
    width: 90vw !important;
  }

  div.banner__box h2.banner__heading {
    font-size: 44px;
    line-height: 44px;
  }

   .wishlist-btn span {
    display: none;
  }

  .new-layout--collection-mobile-2 {
    grid-template-columns: repeat(2, 1fr) !important;
  }

  .new-layout--collection-mobile-1 {
    grid-template-columns: repeat(1, 1fr) !important;
  }

  .new-layout--collection-mobile-2 .new-layout--grit-item {
    width: clamp(220px,20vw,350px) !important;
  }

  .custom-responsive-font:nth-child(3), .custom-responsive-font:nth-child(4) {
    left: 10px;
    bottom: 10px;
    font-size: clamp(16px, 3.5vw, 20px);
  }

  .new-collection--title {
    font-size: 44px;
    line-height: 48px;
  }

  .featured-collection--mobile-layout {
    display: none;
  }
  
}

@media only screen and (max-width: 767px) {
    .footer-custom--mixed {
        width: 100%;
        max-width: 100%;
        margin: 0;
    }

    .footer-block--mixed-img {
        width: 150px;
    }

    .custom-ig--instagram-content {
        flex-direction: column;
        padding: 40px 0;
    }

    .custom-ig--redirect {
        justify-content: center;
    }
    
    .custom-ig--slide {
        max-height: 33.33vw;
    }

    .custom-ig--img {
        width: 33.33vw;
        aspect-ratio: 1/1;
    }

    .info__column-container {
        display: flex !important;
        flex-direction: column !important;
        align-items: center !important;
    }

  .custom-flex--img-box {
    width: 80vw;
    height: auto;
  }

  .new-flex-content-box {
    max-width: fit-content !important;
    padding: 0 15px;
  }

  .footer-custom--blocks-container {
    padding: 30px;
  }

  .grid__item.custom-related--card {
    width: 100%;
    max-width: fit-content;
  }

  ul.grid.custom-grid--related {
    flex-direction: column;
    align-items: center;
    gap: 50px;
  }

  .new-collectio--filster-tag {
    font-size: 14px;
    padding: 10px;
  }
}

@media only screen and (min-width: 768px) {
  .featured-collection--mobile-layout {
    display: none;
  }
}

@media only screen and (min-width: 768px) and (max-width: 1023px) {
  .footer-custom--mixed {
      width: 100%;
      max-width: 100%;
      margin: 0;
  }

  .footer-block--mixed-img {
      width: 210px;
  }

  .footer-block--mixed-flex h2 {
      font-size: 52px;
  }

  .footer-block--mixed-text p {
      font-size: 20px;
  }

  .custom-ig--img {
      width: 25vw;
      aspect-ratio: 1/1;
  }

  .custom-ig--slide {
      max-height: 25vw;
  }

  .product-grid-container .collection {
    max-width: 75vw;
    margin: 0 auto;
  }

  .new-grid--align {
    justify-content: space-around;
  }

  .grid-blocks-align {
    width: 100%;
    max-width: fit-content;
    column-gap: 10vw;
  }

  .ig-btn--new-height {
    min-width: 255px;
  }

  .thumbnail-slider.slider-mobile-gutter {
  margin-left: 0;
  }

  .featured--new-title {
    left: 5%;
  }

  .featured-collections__col.custom-responsive-font:first-child h3, 
  .featured-collections__col.custom-responsive-font:nth-child(2) h3 {
    writing-mode: vertical-lr;
  }
}

@media only screen and (min-width: 768px) and (max-width: 900px) {
  .custom-ig--text-flex h1 {
      font-size: 26px;
  }

  .custom-ig--instagram-content {
      padding: 40px 0;
      max-width: 90%;
  }

  .custom-ig--text-flex p {
      font-size: 16;
  }

  .custom-ig--redirect p {
      font-size: 16px;
  }

  .custom-ig--redirect {
      padding: 20px;
  }

  .info__column-container {
    display: flex !important;
    flex-direction: column !important;
    align-items: center !important;
  }

  .no-presence {
    display: block;
  }

  .wishlist-btn span {
    display: none;
  }

  .product.product--large.product--left.product--thumbnail_slider.product--mobile-columns.grid.grid--1-col.grid--2-col-tablet {
    flex-direction: column;
    gap: 25px;
  }

  .grid__item.product__media-wrapper {
    width: 75vw;
    margin: 0 auto;
  }

  .product__info-wrapper.grid__item.scroll-trigger.animate--slide-in {
    width: 100%;
    padding: 0;
  }

  .product__info-container.product__column-sticky {
    max-width: 100%;
  }

  .custom-rating--container {
    justify-content: space-between;
    width: 100%;
  }

  .shopify-payment-button__button.shopify-payment-button__button--unbranded, .new-btn--class {
    max-width: 44rem;
    width: 100vw;
  }

  .wishlist-btn-box {
    min-width: 66px;
  }

  .custom-slider--top-content h1 {
    max-width: 77%;
    font-size: 34px;
  }

  .new-layout--collection-mobile-2 {
    grid-template-columns: repeat(2, 1fr) !important;
  }

   .new-layout--collection-mobile-1 {
    grid-template-columns: repeat(1, 1fr) !important;
  }

}

@media only screen and (min-width: 901px) and (max-width: 1023px) {
    .custom-ig--instagram-content {
        margin: 50px auto;
    }

    .custom-ig--text-flex h1 {
        font-size: 30px;
    }

    .custom-ig--instagram-content {
        padding: 40px 0;
        max-width: 90%;
    }

  .quantity-checkout--container {
    flex-direction: column;
  }

  .shopify-payment-button__button.shopify-payment-button__button--unbranded, .new-btn--class {
    max-width: 100%;
    width: 100%;
  }

  .wishlist-btn {
    gap: 15px;
    width: 100%;
    display: flex;
    position: relative;
    text-align: center;
    align-items: center;
    justify-content: center;
  }

  .wishlist-btn span {
    font-size: 16px;
    color: var(--color-black);
  }

  .wishlist-btn svg {
    position: absolute;
    right: 10px;
  }

  .row-container {
    padding: 40px;
  }
}

@media only screen and (min-width: 1024px) and (max-width: 1120px) {
    .custom-ig--instagram-content {
        margin: 50px auto;
    }

    .custom-ig--text-flex h1 {
        font-size: 35px;
    }

    .custom-ig--instagram-content {
        padding: 30px 0;
        max-width: 90%;
    }

  .custom-flex--centering {
    width: 100%;
    padding: 0 40px;
  }

  .custom-flex--img-box {
    height: auto;
    width: auto;
  }
}

@media only screen and (max-width: 1023px) {
  .footer__blocks-wrapper.grid.between {
    flex-direction: column;
  }

  .no-presence {
    display: block;
  }

  .custom-flex--image-left {
    display: none;
  }

  .custom-flex--main-content-box {
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }

  .new-email--first-flex {
    flex-direction: column;
    gap: 30px;
  }

  .new-email--first-flex h1, .new-email--first-flex p {
    text-align: center !important;
  }

  facet-filters-form.small-hide {
    display: none !important;
  }

  .facets-vertical.page-width {
    display: flex;
    margin: 0 30px;
    flex-direction: column !important;
  }

  .new-flex--direct-elements-box {
    flex-direction: column;
  }

   .countdown__inner-container {
  flex-direction: column;
  align-items: center;
  gap: 15px;
}

  .countdown__col.countdown--heading {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
  }
  
  .countdown__heading {
    margin: 0 !important;
    text-align: center;
  }

  p.countdown__desc {
    text-align: center;
  }

  .new-layout--card {
    padding: 0 !important;
  }

  .countdown-btn {
      margin: 20px 0 0;
  }
}

@media only screen and (min-width: 1024px) {
  .new--medium-hide {
    display: none !important;
  }

  .wishlist-btn span {
    display: none;
  }

  .banner__box.content-container.content-container--full-width-mobile.color-inverse {
    max-width: 100%;
  }

  .new-layout--product-select {
    max-width: 100% !important;
  }
}

@media only screen and (min-width: 1024px) and (max-width: 1120px) {
  .second-width--container {
    max-width: 85vw;
  }
}

@media only screen and (min-width: 1121px) and (max-width: 1280px) {
  
}

@media only screen and (min-width: 1024px) and (max-width: 1280px) {
  form.newsletter-form {
    width: 430px !important;
  }

  .new-email--submit-btn {
    min-wdth: 150px !important;
  }

  .new-email--first-flex {
    gap: 60px;
    padding: 0 40px;
  }

  .new-email--flex-container h1 {
    font-size: 28px;
  }

  .facets-vertical.page-width, .facets.facets-vertical-sort.page-width {
    padding: 0 5rem;
  }

  .new-cart--inner {
    min-height: 26vw !important;
  }

  .new-grid--align .new-grid--item, .facets__form-vertical {
    max-width: 20vw !important;
  }

  .shopify-payment-button__button.shopify-payment-button__button--unbranded, .new-btn--class  {
    width: calc(200px + (320 - 200) * ((100vw - 1024px) / (1280 - 1024))) !important;
  }

  .simple-product-pic {
    width: 100%;
  }

  .thumbnail-slider.slider-mobile-gutter {
    margin-left: 0;   
  }
}

@media only screen and (min-width: 768px) and (max-width: 1280px) {
    .footer-custom--blocks-container {
        padding: 100px 60px 5rem;
    }
}

@media only screen and (min-width: 1281px) {
  .product-media-container.constrain-height.media-fit-contain {
    max-height: 578px !important;
    width: 552px !important;
  }
}